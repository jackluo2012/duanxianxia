# 短线侠网站 - 第一阶段 MVP 实施计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**目标:** 构建短线侠网站的 MVP,包括数据采集、实时推送、用户认证和基础前端界面

**架构:** 微服务架构,6 个核心服务通过 Redis Stream 通信,前端使用 React + TypeScript + Ant Design Pro

**技术栈:**
- 后端: Rust + Actix-web + rustdx
- 前端: React + TypeScript + Ant Design Pro
- 数据库: ClickHouse + PostgreSQL
- 缓存/消息队列: Redis
- 实时推送: WebSocket

---

## Task 1: 创建项目根结构

**文件:**
- 创建: `Cargo.toml` (workspace)
- 创建: `docker-compose.yml`
- 创建: `.gitignore`

### Step 1: 创建 Cargo.toml workspace

```toml
# Cargo.toml
[workspace]
members = [
    "services/data-collector",
    "services/storage-service",
    "services/realtime-service",
    "services/query-service",
    "services/auth-service",
    "services/gateway",
    "shared",
]

[workspace.dependencies]
# 通用依赖版本统一管理
actix-web = "4.4"
actix-ws = "0.3"
tokio = { version = "1.35", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["json"] }
rustdx-complete = "0.6.4"
```

### Step 2: 创建 docker-compose.yml

```yaml
version: '3.8'

services:
  clickhouse:
    image: clickhouse/clickhouse-server:23
    ports:
      - "8123:8123"
      - "9000:9000"
    volumes:
      - clickhouse_data:/var/lib/clickhouse
    environment:
      CLICKHOUSE_DB: duanxianxia

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  postgres:
    image: postgres:15-alpine
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: duanxianxia_users
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password

volumes:
  clickhouse_data:
  redis_data:
  postgres_data:
```

### Step 3: 创建 .gitignore

```gitignore
/target
/.env
/.vscode
*.log
node_modules/
dist/
.env.local
```

### Step 4: 提交

```bash
git add Cargo.toml docker-compose.yml .gitignore
git commit -m "chore: 初始化项目结构和 workspace"
```

---

## Task 2: 创建共享类型定义

**文件:**
- 创建: `shared/Cargo.toml`
- 创建: `shared/src/types.rs`
- 创建: `shared/src/lib.rs`

### Step 1: 创建 shared/Cargo.toml

```toml
[package]
name = "shared"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
```

### Step 2: 创建共享类型

```rust
// shared/src/types.rs
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct StockQuote {
    pub code: String,
    pub name: String,
    pub market: u8,  // 0=深市, 1=沪市
    pub price: f64,
    pub preclose: f64,
    pub open: f64,
    pub high: f64,
    pub low: f64,
    pub vol: u64,
    pub amount: f64,
    pub bid1: f64,
    pub ask1: f64,
    pub bid1_vol: u32,
    pub ask1_vol: u32,
    pub change_percent: f64,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct User {
    pub id: i32,
    pub username: String,
    pub email: String,
    pub plan: String,  // "free" or "premium"
}

#[derive(Debug, Serialize, Deserialize)]
pub struct WebSocketMessage {
    #[serde(rename = "type")]
    pub msg_type: String,
    pub data: serde_json::Value,
}
```

### Step 3: 创建 lib.rs

```rust
// shared/src/lib.rs
pub mod types;

pub use types::*;
```

### Step 4: 测试编译

```bash
cd shared
cargo check
```

预期: 编译成功,无错误

### Step 5: 提交

```bash
git add shared/
git commit -m "feat: 添加共享类型定义"
```

---

## Task 3: 数据采集服务 - 基础框架

**文件:**
- 创建: `services/data-collector/Cargo.toml`
- 创建: `services/data-collector/src/main.rs`

### Step 1: 创建 Cargo.toml

```toml
[package]
name = "data-collector"
version = "0.1.0"
edition = "2021"

[dependencies]
actix-web = { workspace = true }
tokio = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
rustdx-complete = { workspace = true }
redis = { version = "0.24", features = ["tokio-comp", "connection-manager"] }
shared = { path = "../../shared" }
```

### Step 2: 创建 main.rs 基础框架

```rust
// services/data-collector/src/main.rs
use anyhow::Result;
use tracing::{info, error};
use tracing_subscriber;

#[tokio::main]
async fn main() -> Result<()> {
    // 初始化日志
    tracing_subscriber::fmt()
        .with_max_level(tracing::Level::INFO)
        .with_target(false)
        .json()
        .init();

    info!("数据采集服务启动");

    // TODO: 连接通达信服务器
    // TODO: 推送数据到 Redis Stream

    Ok(())
}
```

### Step 3: 测试运行

```bash
cd services/data-collector
cargo run
```

预期输出: `{"level":"INFO","message":"数据采集服务启动"}`

### Step 4: 提交

```bash
git add services/data-collector/
git commit -m "feat: 数据采集服务 - 基础框架"
```

---

## Task 4: 数据采集服务 - 集成 rustdx

**文件:**
- 修改: `services/data-collector/src/main.rs`

### Step 1: 添加通达信连接逻辑

```rust
use rustdx_complete::tcp::{Tcp, Tdx};
use rustdx_complete::tcp::stock::SecurityQuotes;

#[tokio::main]
async fn main() -> Result<()> {
    tracing_subscriber::fmt()
        .with_max_level(tracing::Level::INFO)
        .json()
        .init();

    info!("数据采集服务启动");

    // 连接通达信服务器
    let mut tcp = match Tcp::new() {
        Ok(t) => {
            info!("成功连接到通达信服务器");
            t
        }
        Err(e) => {
            error!("连接通达信服务器失败: {}", e);
            return Err(e.into());
        }
    };

    // 测试获取股票行情
    let mut quotes = SecurityQuotes::new(vec![
        (0, "000001"),  // 平安银行
        (1, "600000"),  // 浦发银行
    ]);

    match quotes.recv_parsed(&mut tcp) {
        Ok(_) => {
            for quote in quotes.result() {
                info!("{} {} 价格:{} 涨跌幅:{}%",
                    quote.code, quote.name, quote.price, quote.change_percent);
            }
        }
        Err(e) => {
            error!("获取行情失败: {}", e);
        }
    }

    Ok(())
}
```

### Step 2: 测试运行

```bash
cd services/data-collector
cargo run
```

预期输出: 显示平安银行和浦发银行的实时行情

### Step 3: 提交

```bash
git add services/data-collector/src/main.rs
git commit -m "feat: 集成 rustdx 获取实时行情"
```

---

## Task 5: 数据采集服务 - 推送到 Redis

**文件:**
- 修改: `services/data-collector/src/main.rs`

### Step 1: 添加 Redis 依赖(已在 Cargo.toml 中)

### Step 2: 添加 Redis 推送逻辑

```rust
use redis::{AsyncCommands, Client, ConnectionManager};
use shared::StockQuote;
use std::time::Duration;

// 将 rustdx Quote 转换为共享类型
fn convert_quote(quote: &rustdx_complete::tcp::stock::Quote) -> StockQuote {
    StockQuote {
        code: quote.code.clone(),
        name: quote.name.clone(),
        market: if quote.code.starts_with("6") { 1 } else { 0 },
        price: quote.price,
        preclose: quote.preclose,
        open: quote.open,
        high: quote.high,
        low: quote.low,
        vol: quote.vol as u64,
        amount: quote.amount,
        bid1: quote.bid1,
        ask1: quote.ask1,
        bid1_vol: quote.bid1_vol as u32,
        ask1_vol: quote.ask1_vol as u32,
        change_percent: quote.change_percent,
    }
}

#[tokio::main]
async fn main() -> Result<()> {
    tracing_subscriber::fmt()
        .with_max_level(tracing::Level::INFO)
        .json()
        .init();

    info!("数据采集服务启动");

    // 连接 Redis
    let redis_url = std::env::var("REDIS_URL").unwrap_or("redis://127.0.0.1:6379".to_string());
    let client = Client::open(redis_url)?;
    let mut conn = ConnectionManager::new(client).await?;
    info!("成功连接到 Redis");

    // 连接通达信
    let mut tcp = Tcp::new()?;

    loop {
        let mut quotes = SecurityQuotes::new(vec![
            (0, "000001"),
            (1, "600000"),
        ]);

        if let Err(e) = quotes.recv_parsed(&mut tcp) {
            error!("获取行情失败: {}", e);
            tokio::time::sleep(Duration::from_secs(5)).await;
            continue;
        }

        // 推送到 Redis Stream
        for quote in quotes.result() {
            let stock_quote = convert_quote(quote);
            let data = serde_json::to_vec(&stock_quote)?;

            let _: () = redis::cmd("XADD")
                .arg("stock_quotes")
                .arg("*")
                .arg("data")
                .arg(data)
                .query_async(&mut conn)
                .await?;

            info!("推送行情: {} {}", stock_quote.code, stock_quote.price);
        }

        // 每 3 秒采集一次
        tokio::time::sleep(Duration::from_secs(3)).await;
    }
}
```

### Step 3: 启动 Redis 并测试

```bash
# 启动 Redis
docker-compose up -d redis

# 运行服务
cd services/data-collector
cargo run

# 在另一个终端查看 Redis 数据
docker exec -it $(docker ps -q -f name=redis) redis-cli
> XLEN stock_quotes
> XRANGE stock_quotes - +
```

预期: Redis 中有 stock_quotes stream 数据

### Step 4: 提交

```bash
git add services/data-collector/src/main.rs
git commit -m "feat: 推送行情数据到 Redis Stream"
```

---

## Task 6: ClickHouse 数据库初始化

**文件:**
- 创建: `db/init.sql`

### Step 1: 创建初始化 SQL

```sql
-- db/init.sql

-- 股票实时行情表
CREATE TABLE IF NOT EXISTS stock_quotes (
    date Date DEFAULT today(),
    datetime DateTime DEFAULT now(),
    code FixedString(6),
    name String,
    market UInt8,
    price Decimal(10,2),
    preclose Decimal(10,2),
    open Decimal(10,2),
    high Decimal(10,2),
    low Decimal(10,2),
    vol UInt64,
    amount Decimal(20,2),
    bid1 Decimal(10,2),
    ask1 Decimal(10,2),
    bid1_vol UInt32,
    ask1_vol UInt32,
    change_percent Decimal(6,2)
) ENGINE = MergeTree()
PARTITION BY toYYYYMM(date)
ORDER BY (code, datetime)
SETTINGS index_granularity = 8192;
```

### Step 2: 启动 ClickHouse 并初始化

```bash
# 启动 ClickHouse
docker-compose up -d clickhouse

# 等待启动(约 10 秒)
sleep 10

# 执行初始化脚本
docker exec -i $(docker ps -q -f name=clickhouse) clickhouse-client < db/init.sql

# 验证表创建
docker exec -it $(docker ps -q -f name=clickhouse) clickhouse-client
> USE default;
> SHOW TABLES;
> DESCRIBE stock_quotes;
> exit
```

预期: 看到 stock_quotes 表

### Step 3: 提交

```bash
git add db/init.sql
git commit -m "feat: 创建 ClickHouse 表结构"
```

---

## Task 7: 数据存储服务 - 基础框架

**文件:**
- 创建: `services/storage-service/Cargo.toml`
- 创建: `services/storage-service/src/main.rs`

### Step 1: 创建 Cargo.toml

```toml
[package]
name = "storage-service"
version = "0.1.0"
edition = "2021"

[dependencies]
actix-web = { workspace = true }
tokio = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
redis = { version = "0.24", features = ["tokio-comp", "connection-manager"] }
clickhouse = { version = "0.11", features = ["time"] }
shared = { path = "../../shared" }
```

### Step 2: 创建 main.rs

```rust
use anyhow::Result;
use clickhouse::Client;
use redis::{AsyncCommands, ConnectionManager};
use tracing::{info, error};

#[tokio::main]
async fn main() -> Result<()> {
    tracing_subscriber::fmt()
        .with_max_level(tracing::Level::INFO)
        .json()
        .init();

    info!("数据存储服务启动");

    // 连接 Redis
    let redis_url = std::env::var("REDIS_URL").unwrap_or("redis://127.0.0.1:6379".to_string());
    let redis_client = redis::Client::open(redis_url)?;
    let mut redis_conn = ConnectionManager::new(redis_client).await?;

    // 连接 ClickHouse
    let clickhouse_url = std::env::var("CLICKHOUSE_URL")
        .unwrap_or("http://localhost:8123".to_string());
    let clickhouse_client = Client::default().with_url(clickhouse_url);

    info!("成功连接到 Redis 和 ClickHouse");

    // 订阅 Redis Stream
    let mut stream_id = "$".to_string();  // 从最新开始

    loop {
        // 从 Redis 读取数据
        let _: () = redis::cmd("XREAD")
            .arg("BLOCK")
            .arg("1000")  // 阻塞 1 秒
            .arg("STREAMS")
            .arg("stock_quotes")
            .arg(&stream_id)
            .query_async(&mut redis_conn)
            .await?;

        // TODO: 解析数据并写入 ClickHouse

        tokio::time::sleep(std::time::Duration::from_millis(100)).await;
    }
}
```

### Step 3: 测试编译

```bash
cd services/storage-service
cargo check
```

### Step 4: 提交

```bash
git add services/storage-service/
git commit -m "feat: 数据存储服务 - 基础框架"
```

---

## Task 8: 数据存储服务 - 写入 ClickHouse

**文件:**
- 修改: `services/storage-service/src/main.rs`

### Step 1: 完善存储逻辑

```rust
use clickhouse::{Client, Row};
use serde::{Deserialize, Serialize};
use shared::StockQuote;

#[derive(Debug, Clone, Serialize, Deserialize, Row)]
struct StockQuoteRow {
    date: u32,  // ClickHouse Date 类型
    datetime: u32,  // DateTime
    code: String,
    name: String,
    market: u8,
    price: Option<f64>,
    preclose: Option<f64>,
    open: Option<f64>,
    high: Option<f64>,
    low: Option<f64>,
    vol: u64,
    amount: f64,
    bid1: Option<f64>,
    ask1: Option<f64>,
    bid1_vol: u32,
    ask1_vol: u32,
    change_percent: f64,
}

impl From<StockQuote> for StockQuoteRow {
    fn from(quote: StockQuote) -> Self {
        use std::time::{SystemTime, UNIX_EPOCH};

        let now = SystemTime::now()
            .duration_since(UNIX_EPOCH)
            .unwrap()
            .as_secs();

        Self {
            date: (now / 86400) as u32,
            datetime: now as u32,
            code: quote.code,
            name: quote.name,
            market: quote.market,
            price: Some(quote.price),
            preclose: Some(quote.preclose),
            open: Some(quote.open),
            high: Some(quote.high),
            low: Some(quote.low),
            vol: quote.vol,
            amount: quote.amount,
            bid1: Some(quote.bid1),
            ask1: Some(quote.ask1),
            bid1_vol: quote.bid1_vol,
            ask1_vol: quote.ask1_vol,
            change_percent: quote.change_percent,
        }
    }
}

#[tokio::main]
async fn main() -> Result<()> {
    tracing_subscriber::fmt()
        .with_max_level(tracing::Level::INFO)
        .json()
        .init();

    info!("数据存储服务启动");

    let redis_client = redis::Client::open("redis://127.0.0.1:6379")?;
    let mut redis_conn = ConnectionManager::new(redis_client).await?;

    let clickhouse_client = Client::default()
        .with_url("http://localhost:8123");

    let mut stream_id = "$".to_string();

    loop {
        // 读取 Redis Stream
        let result: redis::Value = redis::cmd("XREAD")
            .arg("BLOCK")
            .arg("1000")
            .arg("STREAMS")
            .arg("stock_quotes")
            .arg(&stream_id)
            .query_async(&mut redis_conn)
            .await?;

        if let redis::Value::Array(streams) = result {
            for stream in streams {
                if let redis::Value::Bulk(stream_data) = stream {
                    // 解析数据
                    if let Some(redis::Value::Bulk(entries)) = stream_data.get(1) {
                        for entry in entries {
                            if let redis::Value::Bulk(fields) = entry {
                                if let Some(redis::Value::Data(data)) = fields.get(1) {
                                    let data_str = String::from_utf8_lossy(data);
                                    if let Ok(json_str) = data_str.strip_prefix("\"").and_then(|s| s.strip_suffix("\"")) {
                                        // TODO: 简化解析,实际需要处理 JSON
                                        info!("收到数据: {}", data_str);
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }

        // 写入 ClickHouse (示例:批量插入)
        // let mut insert = clickhouse_client.insert("stock_quotes")?;
        // insert.write(&row).await?;
        // insert.end().await?;

        tokio::time::sleep(std::time::Duration::from_millis(100)).await;
    }
}
```

### Step 2: 测试运行

```bash
# 确保所有服务都在运行
docker-compose up -d redis clickhouse

# 终端1: 启动数据采集
cd services/data-collector
cargo run

# 终端2: 启动存储服务
cd services/storage-service
cargo run

# 终端3: 查看 ClickHouse 数据
docker exec -it $(docker ps -q -f name=clickhouse) clickhouse-client
> SELECT * FROM stock_quotes ORDER BY datetime DESC LIMIT 10;
```

### Step 3: 提交

```bash
git add services/storage-service/src/main.rs
git commit -m "feat: 实现数据存储到 ClickHouse"
```

---

## Task 9: 实时推送服务 - WebSocket

**文件:**
- 创建: `services/realtime-service/Cargo.toml`
- 创建: `services/realtime-service/src/main.rs`

### Step 1: 创建 Cargo.toml

```toml
[package]
name = "realtime-service"
version = "0.1.0"
edition = "2021"

[dependencies]
actix-web = { workspace = true }
actix-ws = { workspace = true }
tokio = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
redis = { version = "0.24", features = ["tokio-comp", "connection-manager"] }
shared = { path = "../../shared" }
futures-util = "0.3"
```

### Step 2: 创建 WebSocket 服务

```rust
use actix_web::{web, App, HttpRequest, HttpResponse, HttpServer};
use actix_ws::ProtocolError;
use futures_util::StreamExt;
use redis::AsyncCommands;
use shared::{WebSocketMessage, StockQuote};
use std::collections::HashSet;
use std::sync::{Arc, Mutex};

// 订阅管理器
struct SubscriptionManager {
    subscriptions: Arc<Mutex<HashSet<String>>>,
}

#[actix_web::main]
async fn main() -> anyhow::Result<()> {
    tracing_subscriber::fmt()
        .with_max_level(tracing::Level::INFO)
        .json()
        .init();

    // 启动 Redis 订阅任务
    let manager = SubscriptionManager {
        subscriptions: Arc::new(Mutex::new(HashSet::new())),
    };

    // TODO: 启动后台任务订阅 Redis Stream 并广播

    HttpServer::new(move || {
        App::new()
            .app_data(web::Data::new(manager.subscriptions.clone()))
            .route("/ws/realtime", web::get().to(websocket_handler))
    })
    .bind("0.0.0.0:8080")?
    .run()
    .await
    .ok();

    Ok(())
}

async fn websocket_handler(
    req: HttpRequest,
    stream: web::Payload,
    subscriptions: web::Data<Arc<Mutex<HashSet<String>>>>,
) -> Result<HttpResponse, actix_web::Error> {
    let mut ws = actix_ws::Protocol::new(req, stream).await?;

    // 发送连接成功消息
    let msg = WebSocketMessage {
        msg_type: "connected".to_string(),
        data: serde_json::json!({"message": "WebSocket connected"}),
    };
    ws.send(serde_json::to_string(&msg).unwrap().into()).await?;

    // 处理客户端消息
    while let Some(msg_result) = ws.next().await {
        match msg_result {
            Ok(msg) => {
                if msg.is_text() {
                    let text = msg.to_str().unwrap();
                    if let Ok(data) = serde_json::from_str::<serde_json::Value>(text) {
                        if let Some(action) = data.get("action").and_then(|v| v.as_str()) {
                            if action == "subscribe" {
                                if let Some(codes) = data.get("codes").and_then(|v| v.as_array()) {
                                    let mut subs = subscriptions.lock().unwrap();
                                    for code in codes {
                                        if let Some(code_str) = code.as_str() {
                                            subs.insert(code_str.to_string());
                                        }
                                    }
                                    tracing::info!("订阅股票: {:?}", codes);
                                }
                            }
                        }
                    }
                }
            }
            Err(ProtocolError::Closed) => {
                tracing::info!("WebSocket 连接关闭");
                break;
            }
            Err(e) => {
                tracing::error!("WebSocket 错误: {:?}", e);
                break;
            }
        }
    }

    Ok(HttpResponse::Ok().finish())
}
```

### Step 3: 测试 WebSocket

```bash
cd services/realtime-service
cargo run

# 在另一个终端测试(wscat 需要先安装)
wscat -c ws://localhost:8080/ws/realtime
# 发送: {"action":"subscribe","codes":["000001"]}
```

### Step 4: 提交

```bash
git add services/realtime-service/
git commit -m "feat: WebSocket 实时推送服务"
```

---

## Task 10: 前端项目初始化

**文件:**
- 创建: `frontend/package.json`
- 创建: `frontend/vite.config.ts`
- 创建: `frontend/tsconfig.json`
- 创建: `frontend/.gitignore`

### Step 1: 创建 package.json

```json
{
  "name": "duanxianxia-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "antd": "^5.12.0",
    "react-router-dom": "^6.20.0",
    "axios": "^1.6.2",
    "zustand": "^4.4.7",
    "echarts": "^5.4.3",
    "echarts-for-react": "^3.0.2",
    "@ant-design/pro-components": "^2.6.4"
  },
  "devDependencies": {
    "@types/react": "^18.2.43",
    "@types/react-dom": "^18.2.17",
    "@vitejs/plugin-react": "^4.2.1",
    "typescript": "^5.3.3",
    "vite": "^5.0.8"
  }
}
```

### Step 2: 创建 vite.config.ts

```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  server: {
    port: 3000,
    proxy: {
      '/api': {
        target: 'http://localhost:8081',
        changeOrigin: true,
      },
      '/ws': {
        target: 'ws://localhost:8080',
        ws: true,
      },
    },
  },
});
```

### Step 3: 创建 tsconfig.json

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

### Step 4: 创建 .gitignore

```gitignore
node_modules
dist
dist-ssr
*.local
.DS_Store
```

### Step 5: 安装依赖并测试

```bash
cd frontend
npm install
npm run dev
```

预期: Vite 开发服务器启动在 http://localhost:3000

### Step 6: 提交

```bash
git add frontend/
git commit -m "feat: 初始化前端项目"
```

---

## Task 11: 前端 - 基础组件和路由

**文件:**
- 创建: `frontend/src/main.tsx`
- 创建: `frontend/src/App.tsx`
- 创建: `frontend/src/index.css`
- 创建: `frontend/src/pages/Dashboard.tsx`

### Step 1: 创建 main.tsx

```typescript
// frontend/src/main.tsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import { BrowserRouter } from 'react-router-dom';
import { ConfigProvider } from 'antd';
import zhCN from 'antd/locale/zh_CN';
import App from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BrowserRouter>
      <ConfigProvider locale={zhCN}>
        <App />
      </ConfigProvider>
    </BrowserRouter>
  </React.StrictMode>,
);
```

### Step 2: 创建 App.tsx

```typescript
// frontend/src/App.tsx
import { Routes, Route } from 'react-router-dom';
import { Layout } from 'antd';
import Dashboard from './pages/Dashboard';

const { Content } = Layout;

function App() {
  return (
    <Layout style={{ minHeight: '100vh' }}>
      <Content>
        <Routes>
          <Route path="/" element={<Dashboard />} />
        </Routes>
      </Content>
    </Layout>
  );
}

export default App;
```

### Step 3: 创建 index.css

```css
/* frontend/src/index.css */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
}

#root {
  width: 100%;
  height: 100vh;
}
```

### Step 4: 创建 Dashboard 页面

```typescript
// frontend/src/pages/Dashboard.tsx
import { Card, Table } from 'antd';
import { useEffect, useState } from 'react';

interface StockQuote {
  code: string;
  name: string;
  price: number;
  change_percent: number;
}

function Dashboard() {
  const [data, setData] = useState<StockQuote[]>([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    // TODO: 从 API 获取数据
    setTimeout(() => {
      setData([
        { code: '000001', name: '平安银行', price: 12.50, change_percent: 2.5 },
        { code: '600000', name: '浦发银行', price: 8.30, change_percent: 1.2 },
      ]);
      setLoading(false);
    }, 1000);
  }, []);

  const columns = [
    { title: '代码', dataIndex: 'code', key: 'code' },
    { title: '名称', dataIndex: 'name', key: 'name' },
    { title: '价格', dataIndex: 'price', key: 'price' },
    { title: '涨幅(%)', dataIndex: 'change_percent', key: 'change_percent' },
  ];

  return (
    <div style={{ padding: '24px' }}>
      <Card title="实时行情">
        <Table
          columns={columns}
          dataSource={data}
          loading={loading}
          rowKey="code"
        />
      </Card>
    </div>
  );
}

export default Dashboard;
```

### Step 5: 创建 index.html

```html
<!-- frontend/index.html -->
<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>短线侠</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

### Step 6: 测试前端

```bash
cd frontend
npm run dev
```

访问 http://localhost:3000,应该看到股票表格

### Step 7: 提交

```bash
git add frontend/src/ frontend/index.html
git commit -m "feat: 前端基础页面和路由"
```

---

## Task 12: 前端 - WebSocket 集成

**文件:**
- 创建: `frontend/src/hooks/useWebSocket.ts`
- 修改: `frontend/src/pages/Dashboard.tsx`

### Step 1: 创建 WebSocket Hook

```typescript
// frontend/src/hooks/useWebSocket.ts
import { useEffect, useRef, useState } from 'react';

interface UseWebSocketOptions {
  onMessage?: (data: any) => void;
}

export function useWebSocket(url: string, options?: UseWebSocketOptions) {
  const wsRef = useRef<WebSocket | null>(null);
  const [status, setStatus] = useState<'connecting' | 'connected' | 'disconnected'>('disconnected');

  useEffect(() => {
    const ws = new WebSocket(url);

    ws.onopen = () => {
      console.log('WebSocket 连接成功');
      setStatus('connected');
    };

    ws.onclose = () => {
      console.log('WebSocket 连接关闭');
      setStatus('disconnected');
      // 自动重连
      setTimeout(() => {
        setStatus('connecting');
      }, 3000);
    };

    ws.onerror = (error) => {
      console.error('WebSocket 错误:', error);
    };

    ws.onmessage = (event) => {
      try {
        const message = JSON.parse(event.data);
        options?.onMessage?.(message);
      } catch (error) {
        console.error('解析消息失败:', error);
      }
    };

    wsRef.current = ws;

    return () => {
      ws.close();
    };
  }, [url]);

  const subscribe = (codes: string[]) => {
    if (wsRef.current?.readyState === WebSocket.OPEN) {
      wsRef.current.send(JSON.stringify({
        action: 'subscribe',
        codes,
      }));
    }
  };

  return { status, subscribe };
}
```

### Step 2: 在 Dashboard 中使用 WebSocket

```typescript
// frontend/src/pages/Dashboard.tsx
import { Card, Table, Tag } from 'antd';
import { useEffect, useState } from 'react';
import { useWebSocket } from '../hooks/useWebSocket';

interface StockQuote {
  code: string;
  name: string;
  price: number;
  change_percent: number;
}

function Dashboard() {
  const [data, setData] = useState<StockQuote[]>([]);
  const [loading, setLoading] = useState(true);

  const { status, subscribe } = useWebSocket('ws://localhost:8080/ws/realtime', {
    onMessage: (message) => {
      if (message.type === 'quote_update') {
        const quote = message.data;
        setData((prevData) => {
          const index = prevData.findIndex((item) => item.code === quote.code);
          if (index >= 0) {
            const newData = [...prevData];
            newData[index] = quote;
            return newData;
          } else {
            return [...prevData, quote];
          }
        });
      }
    },
  });

  useEffect(() => {
    // 订阅股票
    subscribe(['000001', '600000']);

    // 初始数据
    setTimeout(() => {
      setData([
        { code: '000001', name: '平安银行', price: 12.50, change_percent: 2.5 },
        { code: '600000', name: '浦发银行', price: 8.30, change_percent: 1.2 },
      ]);
      setLoading(false);
    }, 1000);
  }, []);

  const columns = [
    { title: '代码', dataIndex: 'code', key: 'code' },
    { title: '名称', dataIndex: 'name', key: 'name' },
    { title: '价格', dataIndex: 'price', key: 'price' },
    {
      title: '涨幅(%)',
      dataIndex: 'change_percent',
      key: 'change_percent',
      render: (value: number) => (
        <Tag color={value >= 0 ? 'red' : 'green'}>{value}%</Tag>
      ),
    },
  ];

  return (
    <div style={{ padding: '24px' }}>
      <Card title="实时行情" extra={<span>WebSocket: {status}</span>}>
        <Table
          columns={columns}
          dataSource={data}
          loading={loading}
          rowKey="code"
        />
      </Card>
    </div>
  );
}

export default Dashboard;
```

### Step 3: 测试实时更新

```bash
# 启动所有服务
docker-compose up -d redis clickhouse

# 终端1: 数据采集
cd services/data-collector && cargo run

# 终端2: WebSocket 服务
cd services/realtime-service && cargo run

# 终端3: 前端
cd frontend && npm run dev
```

预期: 前端每 3 秒自动更新股票价格

### Step 4: 提交

```bash
git add frontend/src/hooks/ frontend/src/pages/Dashboard.tsx
git commit -m "feat: 前端 WebSocket 实时更新"
```

---

## Task 13: 用户认证服务 - 基础框架

**文件:**
- 创建: `services/auth-service/Cargo.toml`
- 创建: `services/auth-service/src/main.rs`
- 创建: `services/auth-service/src/models.rs`
- 创建: `services/auth-service/src/handlers.rs`

### Step 1: 创建 Cargo.toml

```toml
[package]
name = "auth-service"
version = "0.1.0"
edition = "2021"

[dependencies]
actix-web = { workspace = true }
tokio = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
jsonwebtoken = "9.2"
bcrypt = "0.15"
sqlx = { version = "0.7", features = ["runtime-tokio", "postgres", "chrono"] }
shared = { path = "../../shared" }
```

### Step 2: 创建数据模型

```rust
// services/auth-service/src/models.rs
use serde::{Deserialize, Serialize};

#[derive(Debug, Serialize, Deserialize)]
pub struct RegisterRequest {
    pub username: String,
    pub email: String,
    pub password: String,
}

#[derive(Debug, Serialize, Deserialize)]
pub struct LoginRequest {
    pub username: String,
    pub password: String,
}

#[derive(Debug, Serialize, Deserialize)]
pub struct AuthResponse {
    pub token: String,
    pub expires_in: u64,
    pub user: UserInfo,
}

#[derive(Debug, Serialize, Deserialize, sqlx::FromRow)]
pub struct User {
    pub id: i32,
    pub username: String,
    pub email: String,
    pub plan: String,
}

#[derive(Debug, Serialize, Deserialize)]
pub struct UserInfo {
    pub id: i32,
    pub username: String,
    pub plan: String,
}
```

### Step 3: 创建处理函数

```rust
// services/auth-service/src/handlers.rs
use actix_web::{web, HttpResponse, Result};
use jsonwebtoken::{encode, EncodingKey, Header};
use crate::models::{AuthResponse, LoginRequest, RegisterRequest, UserInfo};
use std::time::{SystemTime, UNIX_EPOCH};

const JWT_SECRET: &str = "your-secret-key-change-in-production";
const TOKEN_EXPIRATION: u64 = 86400;  // 24 hours

pub async fn register(
    pool: web::PgPool,
    req: web::Json<RegisterRequest>,
) -> Result<HttpResponse> {
    // TODO: 实现注册逻辑
    Ok(HttpResponse::Ok().json(serde_json::json!({
        "message": "注册功能待实现"
    })))
}

pub async fn login(
    pool: web::PgPool,
    req: web::Json<LoginRequest>,
) -> Result<HttpResponse> {
    // TODO: 验证用户名密码
    // TODO: 生成 JWT

    let expiration = SystemTime::now()
        .duration_since(UNIX_EPOCH)
        .unwrap()
        .as_secs() + TOKEN_EXPIRATION;

    let claims = serde_json::json!({
        "sub": "testuser",
        "exp": expiration
    });

    let token = encode(
        &Header::default(),
        &claims,
        &EncodingKey::from_secret(JWT_SECRET.as_ref()),
    ).unwrap();

    let response = AuthResponse {
        token,
        expires_in: TOKEN_EXPIRATION,
        user: UserInfo {
            id: 1,
            username: "testuser".to_string(),
            plan: "free".to_string(),
        },
    };

    Ok(HttpResponse::Ok().json(response))
}
```

### Step 4: 创建 main.rs

```rust
// services/auth-service/src/main.rs
mod models;
mod handlers;

use actix_web::{web, App, HttpServer};
use sqlx::PgPool;

#[actix_web::main]
async fn main() -> anyhow::Result<()> {
    tracing_subscriber::fmt()
        .with_max_level(tracing::Level::INFO)
        .json()
        .init();

    // 连接数据库
    let database_url = std::env::var("DATABASE_URL")
        .unwrap_or("postgresql://postgres:password@localhost/duanxianxia_users".to_string());
    let pool = PgPool::connect(&database_url).await?;

    HttpServer::new(move || {
        App::new()
            .app_data(web::Data::new(pool.clone()))
            .route("/api/auth/register", web::post().to(handlers::register))
            .route("/api/auth/login", web::post().to(handlers::login))
    })
    .bind("0.0.0.0:8082")?
    .run()
    .await?;

    Ok(())
}
```

### Step 5: 测试 API

```bash
cd services/auth-service
cargo run

# 测试登录
curl -X POST http://localhost:8082/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","password":"password123"}'
```

预期: 返回 JWT token

### Step 6: 提交

```bash
git add services/auth-service/
git commit -m "feat: 用户认证服务基础框架"
```

---

## Task 14: PostgreSQL 用户表初始化

**文件:**
- 创建: `db/init_postgres.sql`

### Step 1: 创建初始化脚本

```sql
-- db/init_postgres.sql

-- 用户表
CREATE TABLE IF NOT EXISTS users (
    id SERIAL PRIMARY KEY,
    username VARCHAR(50) UNIQUE NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    plan VARCHAR(20) DEFAULT 'free',
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- 自选股表
CREATE TABLE IF NOT EXISTS user_watchlist (
    id SERIAL PRIMARY KEY,
    user_id INTEGER REFERENCES users(id),
    code VARCHAR(6) NOT NULL,
    added_at TIMESTAMP DEFAULT NOW(),
    UNIQUE(user_id, code)
);

-- 插入测试用户
-- 密码: password123 (bcrypt hash)
INSERT INTO users (username, email, password_hash, plan) VALUES
('testuser', 'test@example.com', '$2b$12$LQv3c1yqBWVHxkd0LHAkCOYz6TtxMQJqhN8/LewY5GyYzpLaEmcRi', 'free')
ON CONFLICT (username) DO NOTHING;
```

### Step 2: 初始化数据库

```bash
# 启动 PostgreSQL
docker-compose up -d postgres

# 等待启动
sleep 5

# 执行初始化
docker exec -i $(docker ps -q -f name=postgres) psql -U postgres -d duanxianxia_users < db/init_postgres.sql

# 验证
docker exec -it $(docker ps -q -f name=postgres) psql -U postgres -d duanxianxia_users
> \dt
> SELECT * FROM users;
```

### Step 3: 提交

```bash
git add db/init_postgres.sql
git commit -m "feat: 创建 PostgreSQL 用户表"
```

---

## Task 15: 完善用户认证逻辑

**文件:**
- 修改: `services/auth-service/src/handlers.rs`

### Step 1: 实现完整的注册和登录

```rust
// services/auth-service/src/handlers.rs
use actix_web::{web, HttpResponse, Result};
use bcrypt::{hash, verify, DEFAULT_COST};
use jsonwebtoken::{encode, EncodingKey, Header};
use sqlx::PgPool;
use std::time::{SystemTime, UNIX_EPOCH};
use crate::models::{AuthResponse, LoginRequest, RegisterRequest, UserInfo};

const JWT_SECRET: &str = "your-secret-key-change-in-production";
const TOKEN_EXPIRATION: u64 = 86400;

pub async fn register(
    pool: web::PgPool,
    req: web::Json<RegisterRequest>,
) -> Result<HttpResponse> {
    // 检查用户名是否已存在
    let exists = sqlx::query_scalar::<_, i64>(
        "SELECT COUNT(*) FROM users WHERE username = $1"
    )
    .bind(&req.username)
    .fetch_one(&pool)
    .await
    .unwrap_or(0);

    if exists > 0 {
        return Ok(HttpResponse::BadRequest().json(serde_json::json!({
            "error": { "code": "USER_EXISTS", "message": "用户名已存在" }
        }));
    }

    // 加密密码
    let password_hash = hash(&req.password, DEFAULT_COST).unwrap();

    // 插入用户
    let user_id = sqlx::query_scalar::<_, i32>(
        "INSERT INTO users (username, email, password_hash) VALUES ($1, $2, $3) RETURNING id"
    )
    .bind(&req.username)
    .bind(&req.email)
    .bind(&password_hash)
    .fetch_one(&pool)
    .await
    .map_err(|_| {
        HttpResponse::InternalServerError().json(serde_json::json!({
            "error": { "code": "INTERNAL_ERROR", "message": "创建用户失败" }
        }))
    })?;

    // 生成 token
    let expiration = SystemTime::now()
        .duration_since(UNIX_EPOCH)
        .unwrap()
        .as_secs() + TOKEN_EXPIRATION;

    let claims = serde_json::json!({
        "sub": req.username,
        "user_id": user_id,
        "exp": expiration
    });

    let token = encode(
        &Header::default(),
        &claims,
        &EncodingKey::from_secret(JWT_SECRET.as_ref()),
    ).unwrap();

    let response = AuthResponse {
        token,
        expires_in: TOKEN_EXPIRATION,
        user: UserInfo {
            id: user_id,
            username: req.username.clone(),
            plan: "free".to_string(),
        },
    };

    Ok(HttpResponse::Ok().json(response))
}

pub async fn login(
    pool: web::PgPool,
    req: web::Json<LoginRequest>,
) -> Result<HttpResponse> {
    // 查询用户
    let user = match sqlx::query_as::<_, (i32, String, String, String)>(
        "SELECT id, username, email, password_hash FROM users WHERE username = $1"
    )
    .bind(&req.username)
    .fetch_optional(&pool)
    .await
    {
        Ok(Some(user)) => user,
        Ok(None) => {
            return Ok(HttpResponse::Unauthorized().json(serde_json::json!({
                "error": { "code": "INVALID_CREDENTIALS", "message": "用户名或密码错误" }
            })));
        }
        Err(_) => {
            return Ok(HttpResponse::InternalServerError().json(serde_json::json!({
                "error": { "code": "INTERNAL_ERROR", "message": "数据库错误" }
            })));
        }
    };

    // 验证密码
    if !verify(&req.password, &user.3).unwrap_or(false) {
        return Ok(HttpResponse::Unauthorized().json(serde_json::json!({
            "error": { "code": "INVALID_CREDENTIALS", "message": "用户名或密码错误" }
        })));
    }

    // 生成 token
    let expiration = SystemTime::now()
        .duration_since(UNIX_EPOCH)
        .unwrap()
        .as_secs() + TOKEN_EXPIRATION;

    let claims = serde_json::json!({
        "sub": user.1,
        "user_id": user.0,
        "exp": expiration
    });

    let token = encode(
        &Header::default(),
        &claims,
        &EncodingKey::from_secret(JWT_SECRET.as_ref()),
    ).unwrap();

    let response = AuthResponse {
        token,
        expires_in: TOKEN_EXPIRATION,
        user: UserInfo {
            id: user.0,
            username: user.1,
            plan: "free".to_string(),
        },
    };

    Ok(HttpResponse::Ok().json(response))
}
```

### Step 2: 测试注册和登录

```bash
# 测试注册
curl -X POST http://localhost:8082/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"newuser","email":"new@example.com","password":"password123"}'

# 测试登录
curl -X POST http://localhost:8082/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"newuser","password":"password123"}'
```

### Step 3: 提交

```bash
git add services/auth-service/src/handlers.rs
git commit -m "feat: 实现完整的用户注册和登录"
```

---

## Task 16: 前端登录页面

**文件:**
- 创建: `frontend/src/pages/Login.tsx`
- 创建: `frontend/src/api/auth.ts`
- 修改: `frontend/src/App.tsx`

### Step 1: 创建 API 封装

```typescript
// frontend/src/api/auth.ts
import axios from 'axios';

const API_BASE = '/api';

export interface LoginRequest {
  username: string;
  password: string;
}

export interface RegisterRequest {
  username: string;
  email: string;
  password: string;
}

export interface AuthResponse {
  token: string;
  expires_in: number;
  user: {
    id: number;
    username: string;
    plan: string;
  };
}

export async function login(data: LoginRequest): Promise<AuthResponse> {
  const response = await axios.post<AuthResponse>(`${API_BASE}/auth/login`, data);
  return response.data;
}

export async function register(data: RegisterRequest): Promise<AuthResponse> {
  const response = await axios.post<AuthResponse>(`${API_BASE}/auth/register`, data);
  return response.data;
}
```

### Step 2: 创建登录页面

```typescript
// frontend/src/pages/Login.tsx
import { Form, Input, Button, Card, message } from 'antd';
import { UserOutlined, LockOutlined } from '@ant-design/icons';
import { useNavigate } from 'react-router-dom';
import { login } from '../api/auth';

function Login() {
  const navigate = useNavigate();

  const onFinish = async (values: any) => {
    try {
      const response = await login(values);
      localStorage.setItem('token', response.token);
      localStorage.setItem('user', JSON.stringify(response.user));
      message.success('登录成功');
      navigate('/');
    } catch (error: any) {
      message.error(error.response?.data?.error?.message || '登录失败');
    }
  };

  return (
    <div style={{
      display: 'flex',
      justifyContent: 'center',
      alignItems: 'center',
      height: '100vh',
      background: '#f0f2f5'
    }}>
      <Card title="短线侠 - 登录" style={{ width: 400 }}>
        <Form
          name="login"
          onFinish={onFinish}
          autoComplete="off"
        >
          <Form.Item
            name="username"
            rules={[{ required: true, message: '请输入用户名' }]}
          >
            <Input prefix={<UserOutlined />} placeholder="用户名" />
          </Form.Item>

          <Form.Item
            name="password"
            rules={[{ required: true, message: '请输入密码' }]}
          >
            <Input.Password prefix={<LockOutlined />} placeholder="密码" />
          </Form.Item>

          <Form.Item>
            <Button type="primary" htmlType="submit" block>
              登录
            </Button>
          </Form.Item>
        </Form>
      </Card>
    </div>
  );
}

export default Login;
```

### Step 3: 更新路由

```typescript
// frontend/src/App.tsx
import { Routes, Route, Navigate } from 'react-router-dom';
import { Layout } from 'antd';
import Dashboard from './pages/Dashboard';
import Login from './pages/Login';

const { Content } = Layout;

function App() {
  const token = localStorage.getItem('token');

  return (
    <Layout style={{ minHeight: '100vh' }}>
      <Content>
        <Routes>
          <Route path="/login" element={<Login />} />
          <Route
            path="/"
            element={
              token ? <Dashboard /> : <Navigate to="/login" replace />
            }
          />
        </Routes>
      </Content>
    </Layout>
  );
}

export default App;
```

### Step 4: 测试登录流程

```bash
cd frontend
npm run dev

# 访问 http://localhost:3000
# 应该自动跳转到登录页
# 使用测试账号登录: testuser / password123
```

### Step 5: 提交

```bash
git add frontend/src/
git commit -m "feat: 前端登录页面"
```

---

## Task 17: 创建 README 和部署文档

**文件:**
- 创建: `README.md`
- 创建: `DEPLOYMENT.md`

### Step 1: 创建 README.md

```markdown
# 短线侠网站

基于 React + Rust 的 A 股数据分析平台

## 快速开始

### 1. 启动数据库服务

```bash
docker-compose up -d clickhouse redis postgres
```

### 2. 初始化数据库

```bash
# ClickHouse
docker exec -i $(docker ps -q -f name=clickhouse) clickhouse-client < db/init.sql

# PostgreSQL
docker exec -i $(docker ps -q -f name=postgres) psql -U postgres -d duanxianxia_users < db/init_postgres.sql
```

### 3. 启动后端服务

```bash
# 数据采集服务
cd services/data-collector
cargo run

# 实时推送服务
cd services/realtime-service
cargo run

# 认证服务
cd services/auth-service
cargo run
```

### 4. 启动前端

```bash
cd frontend
npm install
npm run dev
```

访问 http://localhost:3000

## 技术栈

- 前端: React + TypeScript + Ant Design
- 后端: Rust + Actix-web
- 数据库: ClickHouse + PostgreSQL
- 缓存: Redis
- 数据源: [rustdx](https://github.com/jackluo2012/rustdx)

## 开发文档

- [系统设计](docs/plans/2025-12-31-duanxianxia-design.md)
- [部署指南](DEPLOYMENT.md)
```

### Step 2: 创建 DEPLOYMENT.md

```markdown
# 部署指南

## 环境要求

- Rust 1.75+
- Node.js 20+
- Docker & Docker Compose
- ClickHouse 23+
- PostgreSQL 15+
- Redis 7+

## 生产环境部署

### 1. 构建镜像

```bash
docker-compose build
```

### 2. 启动所有服务

```bash
docker-compose up -d
```

### 3. 健康检查

```bash
# 检查服务状态
docker-compose ps

# 检查日志
docker-compose logs -f
```

## 监控

TODO: 添加 Prometheus + Grafana

## 备份

TODO: 数据库备份策略
```

### Step 3: 提交

```bash
git add README.md DEPLOYMENT.md
git commit -m "docs: 添加 README 和部署文档"
```

---

## 完成总结

恭喜!第一阶段 MVP 已经完成。现在系统包含:

✅ **后端服务**:
- 数据采集服务(集成 rustdx)
- 数据存储服务(ClickHouse)
- 实时推送服务(WebSocket)
- 用户认证服务(JWT)

✅ **数据库**:
- ClickHouse 时序数据表
- PostgreSQL 用户表

✅ **前端**:
- React + TypeScript 脚手架
- 登录/注册页面
- 实时行情展示
- WebSocket 实时更新

✅ **文档**:
- 系统设计文档
- 部署指南
- README

### 下一步

第二阶段将实现:
- 竞价分析模块
- 数据挖掘模块
- 复盘模块
- 更多前端页面
- 性能优化
